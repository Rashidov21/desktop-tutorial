
function test(){
    let cube = document.querySelector(".cube");
    cube.scrollTop += 50;
    console.log(window.scrollY)
    // cube.offsetWidth  = '400px'
    // cube.offsetHeight = '400px'

}
    // elem.scrollTop >> scrolling qiladi tepadan qochiradi
    // elem.scrollLeft >> scrolling qiladi Chapdan qochiradi
    // elem.scrollY >> oynani scrolling qiladi tepadan qochiradi
    // elem.scrollX >> oynani scrolling qiladi chapdan qochiradi
